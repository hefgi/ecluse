mode = "host"
max_slots = 6
prefix = "ecluse"

# Next.js app: slot 1 → ECLUSE_APP_PORT=3001 + PORT alias, slot 2 → 3002
[[services]]
name = "app"
base_port = 3000

[hooks]
post_up = "npx prisma migrate deploy"
pre_down = "npx prisma migrate reset --force"
