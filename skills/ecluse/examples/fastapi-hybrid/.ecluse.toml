mode = "hybrid"
max_slots = 6
base_port = 8000
stride = 100
prefix = "ecluse"

# FastAPI on index 0 → ECLUSE_API_PORT + PORT alias (e.g. 8100 for slot 1)
# Vue dev server on index 1 → ECLUSE_FRONTEND_PORT (e.g. 8101 for slot 1)
[ports]
api = 0
frontend = 1

[hooks]
on_up = "alembic upgrade head"
