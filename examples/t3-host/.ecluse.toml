mode = "host"
max_slots = 6
prefix = "ecluse"

# Next.js app: slot 1 → PORT=3001, slot 2 → PORT=3002, etc.
[[services]]
name = "app"
base_port = 3000

[hooks]
post_up = "npx prisma migrate deploy"
pre_down = "npx prisma migrate reset --force"
