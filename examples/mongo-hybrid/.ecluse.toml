mode = "hybrid"
max_slots = 6
base_port = 3000
stride = 100
prefix = "ecluse"

# Node/Express API on index 0 → ECLUSE_API_PORT + PORT alias (e.g. 3100 for slot 1)
[ports]
api = 0

[hooks]
on_up = "npm run db:seed"
