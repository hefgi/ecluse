mode = "hybrid"
max_slots = 6
base_port = 3000
stride = 100
prefix = "ecluse"

# Next.js dev server on index 0 → ECLUSE_APP_PORT + PORT alias (e.g. 3100 for slot 1)
[ports]
app = 0

[hooks]
on_up = "npx prisma migrate deploy"
